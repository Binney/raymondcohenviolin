<% provide :title, "Edit image" %>

<h1>Edit image</h1>

<%= form_for @picture do |f| %>

	<%= f.label :url, "Location" %>
	<%= f.text_field :url %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit "Save changes", class: "btn btn-large btn-primary" %>

<% end %>