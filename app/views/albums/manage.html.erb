<% provide :title, "Manage music" %>

<h1>Manage music</h1>

<% if @albums.any? %>
	
	<ul class="albums">
		<% @albums.each do |album| %>
			<%= render "manage_album", album: album %>
		<% end %>
	</ul>

<% else %>

	No albums found.

<% end %>